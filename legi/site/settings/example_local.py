import os

DEBUG = True

ALLOWED_HOSTS = []

SECRET_KEY = FILL_THIS_OUT

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'jshacks-legi',
    }
}

